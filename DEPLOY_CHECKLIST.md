#  Flowers - Деплой чеклист

## ✅ Готово

### Функционал
- [x] Главная страница с Hero-слайдером (5 сек автосмена)
- [x] Каталог товаров с карточками
- [x] Модалка заказа с валидацией (Zod)
- [x] Отправка заказов в Telegram
- [x] Секции: О нас, Доставка, Контакты
- [x] Футер с контактами
- [x] Мобильная адаптация

### Админ-панель (/admin)
- [x] Авторизация по паролю
- [x] CRUD для букетов
- [x] Настройки магазина (контакты)
- [x] Редактирование контента (О нас, Доставка)
- [x] Переключатель Открыто/Закрыто

### Безопасность
- [x] Middleware защита админки
- [x] Валидация форм (Zod)
- [x] Secure cookies для сессии
- [x] Telegram токен на сервере

---

## 📋 Перед деплоем

### 1. Supabase настройка
1. Создай проект на [supabase.com](https://supabase.com)
2. Перейди в SQL Editor
3. Выполни содержимое файла `supabase-setup.sql`
4. Скопируй URL и anon key из Settings → API

### 2. Telegram бот
1. Создай бота через @BotFather
2. Получи токен бота
3. Получи Chat ID (напиши боту, затем открой: `https://api.telegram.org/bot<TOKEN>/getUpdates`)

### 3. Environment variables
Создай `.env.local` (для локальной разработки) и добавь в Vercel:

```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
TELEGRAM_BOT_TOKEN=123456789:ABC...
TELEGRAM_CHAT_ID=-1001234567890
ADMIN_PASSWORD=ВАШ_НАДЕЖНЫЙ_ПАРОЛЬ
```

### 4. Изменить пароль админки
⚠️ **ВАЖНО**: Замени стандартный пароль `diana2024` на свой!
В файле `.env.local` измени `ADMIN_PASSWORD`

### 5. Добавить фото букетов
- Положи фото в `public/` с именами `bouquet-1.jpg` ... `bouquet-6.jpg`
- Для товаров используй Supabase Storage или внешние URL

---

## 🚀 Деплой на Vercel

### Шаг 1: Push на GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/diana-flowers.git
git push -u origin main
```

### Шаг 2: Подключить к Vercel
1. Открой [vercel.com](https://vercel.com)
2. Import Git Repository
3. Выбери репозиторий
4. Добавь Environment Variables (из пункта 3)
5. Deploy!

### Шаг 3: Домен (опционально)
1. В Vercel → Settings → Domains
2. Добавь свой домен
3. Настрой DNS у регистратора

---

## 🔒 Дополнительная безопасность (рекомендуется)

### Rate Limiting
Добавь в `next.config.mjs`:
```js
headers: async () => [
  {
    source: '/api/:path*',
    headers: [
      { key: 'X-RateLimit-Limit', value: '100' }
    ]
  }
]
```

### Content Security Policy
Добавь в `middleware.ts`:
```js
response.headers.set('X-Content-Type-Options', 'nosniff');
response.headers.set('X-Frame-Options', 'DENY');
```

---

## 📱 Тестирование

После деплоя проверь:
- [ ] Главная страница загружается
- [ ] Слайдер работает (ждать 5+ сек)
- [ ] Каталог показывает товары
- [ ] Форма заказа открывается и валидируется
- [ ] Заказ приходит в Telegram
- [ ] Админка доступна по /admin
- [ ] Настройки сохраняются
- [ ] Сайт работает на мобильных

---

## 📞 Поддержка

При проблемах:
1. Проверь консоль браузера (F12)
2. Проверь логи в Vercel → Functions
3. Проверь Supabase → Logs

---

© 2026 Diana Flowers

